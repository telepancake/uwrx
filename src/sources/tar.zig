//! Tarball source (future implementation)

const std = @import("std");
const types = @import("types.zig");

/// Tarball source (placeholder)
pub const TarSource = struct {
    // TODO: Implement tarball source
    // This would:
    // 1. Open and index the tarball
    // 2. Provide read-only access to files within
    // 3. Support optional subpath extraction
};
