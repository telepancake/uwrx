//! OCI image source (future implementation)

const std = @import("std");
const types = @import("types.zig");

/// OCI source (placeholder)
pub const OciSource = struct {
    // TODO: Implement OCI source
    // This would:
    // 1. Pull/access OCI image
    // 2. Extract layers
    // 3. Provide read-only access to files
    // 4. Support optional subpath extraction
};
